name: otel_socket_io_client
description: >-
  OpenTelemetry instrumentation for package:socket_io_client. Wraps emit
  calls in PRODUCER spans and on() handlers in CONSUMER spans following OTel
  messaging semconv.
version: 0.1.0-wip
repository: https://github.com/dartastic/otel_socket_io_client
homepage: https://dartastic.io

environment:
  sdk: ^3.0.0

dependencies:
  dartastic_opentelemetry: ^1.1.0-beta.12
  dartastic_opentelemetry_api: ^1.0.0-rc.1
  socket_io_client: ^3.0.0

dev_dependencies:
  dart_flutter_team_lints: ^3.5.2
  pub_semver: ^2.0.0
  pubspec_parse: ^1.5.0
  test: ^1.26.3
